---
description: Implement or fix a backend feature in Node.js/TypeScript following project guidelines
---

Follow the implementation workflow defined in `.ai/rules/rules.md`.

Design and implement the feature or bug fix described in $ARGUMENTS.

- Restate requirements briefly.
- Plan the solution and identify affected resolvers, services, or models.
- Follow backend project architecture and `.ai/rules/rules.md` if present.
- Write clean, type-safe Node.js/TypeScript code (no `any`).
- Handle database operations gracefully via Prisma.
- Provide structured output with a summary and grouped file changes.
